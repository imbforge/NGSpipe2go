count_breaks_vars=[
    outdir          : RESULTS + "/counts",
    paired          : RUN_IN_PAIRED_END_MODE,
    threads         : Integer.toString(ESSENTIAL_THREADS)
]

load  PIPELINE_ROOT + "/modules/bliss/count_breaks.groovy"

